import React from 'react';
import '../sass/header.scss';

const Header: React.FC = () => {
    return (
        <header className="profile-header">
            <span className="header-title">edu-connect</span>
        </header>
    );
};

export default Header;